/** Re-enable when Forum, Storefront, and Folders are ready for public launch. */
export const PUBLIC_NAV_FEATURES_ENABLED = false;

/** Re-enable when Studio tier checkout should be available. */
export const STUDIO_TIER_PURCHASE_ENABLED = false;
